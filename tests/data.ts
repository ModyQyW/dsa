export const compareNumbers1 = (a: number, b: number) => a - b;
// if (a < b) return -1;
// if (a === b) return 0;
// return 1;

export const compareNumbers2 = (a: number, b: number) => b - a;
// if (a < b) return 1;
// if (a === b) return 0;
// return -1;
